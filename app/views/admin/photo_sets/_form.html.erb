

<% semantic_form_for [:admin, @photo_set] do |f| %>
	
	<% f.inputs do %>
	
		<%= f.input :name, :wrapper_html => { :class => "large" } %>
		<%= f.input :description, :input_html => { :rows => 5 } %>

	<% end %>
	
	<%= f.buttons %>
	
<% end %>