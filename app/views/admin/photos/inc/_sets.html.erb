	<h4>Sets</h4>
	
	<ul>
		<% @category.sets.each do |set| %>
		<li>
			<%= link_to set, admin_set_path(set), { 
				:title => "#{pluralize(set.count, 'photo')} in set" } %>
		</li>
		<% end %>
	</ul>
	