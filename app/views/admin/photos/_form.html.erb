
<!-- We need a multipart form as we're uploading images -->

<% semantic_form_for [:admin, @photo] do |f| %>  

	<% f.inputs do %>
	
	
		<%= f.input :title %>
	
		<!-- For rating the photo -->
		<%= f.input :rating, 
					:as => :radio, 
					:collection => (1..5), 
					:input_html => { :class => "star" } %>

		<%= f.input :category, :collection => Category.all %> 

		<%= f.buttons %>
	
	<% end %>

	
<% end %>
