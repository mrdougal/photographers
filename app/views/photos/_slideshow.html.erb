<div id="slideshow">
	
	<!-- wrapper element for the large image -->
	<div id="image_wrap">

		<% link_to '#', :title => "View photo" do %>
		<!-- Initially the image is a simple 1x1 pixel transparent GIF -->
		<%= image_tag 'shim.gif' %>
		<% end %>


	</div>

	<!-- Title of the photo goes in here -->
	<h4 class="caption"></h4>

	<!-- root element for scrollable -->
	<div class="scrollable">	
	
		<!-- root element for the items -->
		<div class="items">
			<%- @photos.each do |photo| -%>
				<%- link_to photo, :title => "#{photo}" do -%>
					<%= image_tag photo.url(:tiny), { :class => "uploaded" } %>
				<%- end -%>
			<%- end -%>
		</div>
	</div>
</div>