<% title "Photos tagged #{@tag} #{pagination_title @photos}" %>



<% @photos.each do |photo| %>
    <% link_to photo do %>
		<%= image_tag photo.url(:thumb), { :class => "uploaded" } %>
	<% end %>
<% end %>


<%= will_paginate @photos %>

