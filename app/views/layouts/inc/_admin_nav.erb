
<div id="nav">

	<ul>
	<!-- Only display the following if the user is logged in -->
	<% if logged_in? %>
		<li><%= link_to 'Home', admin_path, { 
				:id => "admin_home", 
				:class => admin_current_class(admin_path)} %></li>
		<li><%= link_to 'Photos', admin_photos_path, { :class => admin_current_or_active_class(admin_photos_path)} %></li>
	<% end %>

		<li class="right"><%= link_to 'View Site', root_path, { :id => "site" } %></li>
	</ul>
</div>


